#pragma once

enum class MouseButton {
    // Primary buttons
    left, right, middle,

    // Extra buttons
    button_4, button_5,

    // Wheel input
    wheel_up, wheel_down,

    // Fallback
    unknown
};
